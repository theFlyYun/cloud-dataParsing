#!/usr/bin/env node

var DeviceShadow = require('../index');
var config = require('../config');

var deviceShadow = new DeviceShadow(config);

process.on('SIGINT', function () {//监听服务器运行过程中出现错误
  deviceShadow.close().then(function () {
    console.log('close done')
  })
});
